# Pre-commit hook — runs the project quality gate (lint and type
# check, via vp check) and blocks the commit if it fails.
#
# CWD is the git repo root, where package.json, tsconfig, and
# node_modules live.
#
# set -e so a failing step aborts and git refuses the commit.
set -e

npm run check
